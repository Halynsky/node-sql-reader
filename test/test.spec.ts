import { SqlReader } from "../src";
import * as path from "path";

describe('Test', () => {

    it('should read-sql', () => {
        console.log('Hello world')
        // let queries = SqlReader.readSqlFile(path.join(__dirname, "../data/users.sql"));
        // console.log(queries)
        // expect(queries).toHaveSize(3)
    });



})
